#!/usr/bin/env npx tsx
/**
 * Utility to match unmatched pamphlet candidates to database records
 * 
 * Usage:
 *   npx tsx scripts/utils/match-pamphlet-candidates.ts [year] [--auto]
 * 
 * Options:
 *   --auto    Only show